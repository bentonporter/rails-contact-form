== Steps to Run

1. Download or fork this project.

2. Edit the "config/initializers/app_config.rb" file to configure the application.

3. Run the app on your local machine ("rails server") and hop over to http://localhost:3000/.